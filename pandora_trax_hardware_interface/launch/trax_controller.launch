<launch>
  <arg name="namespace" default="trax" />

  <!-- load configuration -->
  <rosparam
    command="load"
    file="$(find pandora_trax_hardware_interface)/config/trax_sensor_controller.yaml"
    ns="$(arg namespace)"/>

  <!-- spawn controller -->
  <node
    name="imu_controller_spawner"
    pkg="controller_manager"
    type="controller_manager"
    output="screen"
    ns="$(arg namespace)"
    args="spawn imu_sensor_controller"/>
</launch>
